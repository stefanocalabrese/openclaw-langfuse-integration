# =============================================================================
# Langfuse Docker Stack - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# Generate secrets with: openssl rand -base64 32 (or -hex 32 for ENCRYPTION_KEY)
# =============================================================================

# -----------------------------------------------------------------------------
# Database Passwords
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=your-secure-postgres-password
CLICKHOUSE_PASSWORD=your-secure-clickhouse-password
REDIS_PASSWORD=your-secure-redis-password
MINIO_PASSWORD=your-secure-minio-password

# -----------------------------------------------------------------------------
# Langfuse Configuration
# -----------------------------------------------------------------------------
# Your public Langfuse URL (must match what you use in browser/nginx)
LANGFUSE_URL=https://langfuse.yourdomain.com

# Auth secrets - generate with: openssl rand -base64 32
NEXTAUTH_SECRET=generate-me-with-openssl-rand-base64-32
SALT=generate-me-with-openssl-rand-base64-32

# Encryption key - generate with: openssl rand -hex 32
ENCRYPTION_KEY=generate-me-with-openssl-rand-hex-32

# -----------------------------------------------------------------------------
# Initial User & Project Setup (Optional)
# -----------------------------------------------------------------------------
# These create the initial organization, project, and admin user on first run
LANGFUSE_INIT_ORG_NAME=MyOrganization
LANGFUSE_INIT_PROJECT_ID=my-project
LANGFUSE_INIT_PROJECT_NAME=OpenClaw
LANGFUSE_INIT_USER_EMAIL=admin@example.com
LANGFUSE_INIT_USER_NAME=admin
LANGFUSE_INIT_USER_PASSWORD=your-admin-password
